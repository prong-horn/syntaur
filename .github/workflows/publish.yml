name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type"
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write
  pages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          # Pinned exactly, not floating "24". Node 24.19.0 tightened the
          # assertion in RemoveEnvironmentCleanupHook, which better-sqlite3
          # 11.x trips from Statement::~Statement() during worker teardown:
          # a vitest worker dies with `Assertion failed: (env) != nullptr`,
          # tinypool reports "Channel closed", and the release fails with no
          # test having actually failed. 24.18.0 is what shipped 0.79.0.
          # Lift this once better-sqlite3 is upgraded (11.x -> 13.x).
          node-version: "24.18.0"
          registry-url: "https://registry.npmjs.org"

      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@^11.10.0

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build CLI (required for subprocess CLI tests)
        run: npm run build

      - name: Run test suite
        run: npm test

      - name: Verify adapter templates
        run: npx vitest run src/__tests__/adapter-templates.test.ts

      # Guards the tag-push path: the committed plugin manifests must already
      # match package.json (the `version` lifecycle script stamps them on bump).
      - name: Verify plugin version sync
        run: node scripts/sync-plugin-version.mjs --check

      - name: Bump version and push tag (manual dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # `npm version` runs the `version` lifecycle script, which stamps the
          # three plugin manifests and `git add`s them into the bump commit.
          npm version ${{ inputs.bump }} -m "chore: release %s"
          # Validate the POST-bump commit before publishing/pushing.
          node scripts/sync-plugin-version.mjs --check
          git push --follow-tags

      - name: Verify tag matches package version (tag push)
        if: github.event_name == 'push'
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
            echo "Tag version ($TAG_VERSION) does not match package.json version ($PACKAGE_VERSION)."
            exit 1
          fi

      - name: Extract version
        id: extract
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Publish to npm with OIDC
        run: npm publish

      - name: Notify syntaur-app
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.SYNTAUR_APP_DISPATCH_PAT }}
          repository: prong-horn/syntaur-app
          event-type: syntaur-published
          client-payload: '{"version": "${{ steps.extract.outputs.version }}"}'

  # Generate the Agent Skills v0.2.0 discovery index from skills/ and deploy it
  # (plus the per-skill artifacts) to GitHub Pages, so `npx skills add
  # https://prong-horn.github.io/syntaur` resolves + content-verifies Syntaur
  # skills over HTTP. Runs only after `release` (its full test suite is the
  # drift gate) and in the SAME run for both triggers, so it never depends on
  # the bot-pushed tag re-triggering a workflow.
  #
  # One-time prerequisite (required — the default GITHUB_TOKEN cannot enable
  # Pages, so `configure-pages` runs WITHOUT enablement): enable Pages with
  # Source = GitHub Actions once, via Settings → Pages or:
  #   gh api -X POST repos/prong-horn/syntaur/pages -f build_type=workflow
  pages:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          # Kept in step with the release job above.
          node-version: "24.18.0"

      - name: Generate .well-known skills index
        run: node scripts/build-skills-index.mjs --out-dir _site/.well-known/agent-skills

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4

interface:
  display_name: "Syntaur"
  short_description: "Project and ticket workflows for Codex"
  default_prompt: "Use Syntaur to create projects and tickets, claim work, plan an ticket, or complete a handoff in the current workspace."

policy:
  allow_implicit_invocation: true

# ACP adapter transcripts

Recorded JSON-RPC traffic between the spike client (`scripts/spike/acp/`) and the
two ACP adapters Syntaur's assignment chat will drive. Captured by the
`acp-adapter-spike` assignment; regenerate with

```sh
cd scripts/spike/acp
node run.ts --adapter claude --run claude-full
node run.ts --adapter codex  --run codex-full
node fixtures.ts --claude claude-full --codex codex-full
```

## Layout

- `claude/<nn>-<slug>[.label].ndjson` — `claude-agent-acp` transcripts, one per
  spawned adapter process (`label` distinguishes multiple processes in one
  scenario, e.g. `19-concurrency.planner` / `.implementer`).
- `codex/…` — the same for `codex-acp`.
- `manifest.json` — run ids, adapter/CLI versions, the source commit each
  adapter operated on, and for every scenario × adapter: pass/fail, duration,
  notes, headline metrics, source commit, and the transcript files.

## Capture

<!-- capture:start -->
Captured 2026-09-02; regenerated by `fixtures.ts`, do not edit by hand.

| Adapter | Run | Versions | Source commit |
|---|---|---|---|
| claude | `claude-full` | node v22.22.1, claude-agent-acp 0.70.0, codex-acp @agentclientprotocol/codex-acp 1.7.0, claude 2.1.258 (Claude Code), codex codex-cli 0.149.1, @agentclientprotocol/sdk 1.4.0 | `35dbef5d719e` |
| codex | `codex-full` | node v22.22.1, claude-agent-acp 0.70.0, codex-acp @agentclientprotocol/codex-acp 1.7.0, claude 2.1.258 (Claude Code), codex codex-cli 0.149.1, @agentclientprotocol/sdk 1.4.0 | `35dbef5d719e` |

Rows whose source commit differs from the run's were rerun with `--only` after the branch moved.

| Scenario | Adapter | Result | Source commit | Transcripts |
|---|---|---|---|---|
| 02-handshake — Handshake + trivial prompt | claude | PASS | `-` | `claude/02-handshake.ndjson` |
| 03-system-prompt — System prompt transport | claude | PASS | `-` | `claude/03-system-prompt.ndjson` |
| 04-streaming — Streaming / coalescing | claude | PASS | `-` | `claude/04-streaming.ndjson` |
| 05-tool-calls — Tool calls (read + search) | claude | PASS | `35dbef5d719e` | `claude/05-tool-calls.ndjson` |
| 06-edits — Edits / diffs in auto-accept mode | claude | PASS | `-` | `claude/06-edits.ndjson` |
| 07-permissions — Permission requests: allow, reject, and cancel-while-pending | claude | PASS | `-` | `claude/07-permissions.ndjson` |
| 08-plan-events — Plan / todo events | claude | PASS | `-` | `claude/08-plan-events.ndjson` |
| 09-thoughts — Thought chunks at high effort | claude | observed | `-` | `claude/09-thoughts.ndjson` |
| 10-commands-config — Slash commands + config option switch | claude | PASS | `-` | `claude/10-commands-config.ndjson` |
| 11-usage — Usage updates | claude | PASS | `-` | `claude/11-usage.ndjson` |
| 12-cancel — Cancel mid-turn + process hygiene | claude | PASS | `35dbef5d719e` | `claude/12-cancel.inherited.ndjson`, `claude/12-cancel.low.ndjson` |
| 13-queue-vs-steer — Second prompt during a turn; steering | claude | FAIL | `35dbef5d719e` | `claude/13-queue-vs-steer.ndjson` |
| 14-load-resume — session/load and session/resume after adapter restart | claude | PASS | `-` | `claude/14-load-resume.p1.ndjson`, `claude/14-load-resume.p2.ndjson`, `claude/14-load-resume.p3.ndjson` |
| 15-crash — kill -9 mid-turn | claude | PASS | `-` | `claude/15-crash.ndjson`, `claude/15-crash.early-exit.ndjson` |
| 16-embedded-context — Embedded resource block + standing-context cost | claude | PASS | `-` | `claude/16-embedded-context.ndjson` |
| 17-subagent — Sub-agent (Task tool) rendering | claude | observed | `-` | `claude/17-subagent.ndjson` |
| 19-concurrency — Two adapters on the same worktree | claude | PASS | `-` | `claude/19-concurrency.planner.ndjson`, `claude/19-concurrency.implementer.ndjson` |
| 20-client-death — Client closes stdin mid-turn (dashboard/server dies) | claude | PASS | `-` | `claude/20-client-death.ndjson` |
| 02-handshake — Handshake + trivial prompt | codex | PASS | `-` | `codex/02-handshake.ndjson` |
| 03-system-prompt — System prompt transport | codex | PASS | `-` | `codex/03-system-prompt.ndjson` |
| 04-streaming — Streaming / coalescing | codex | PASS | `-` | `codex/04-streaming.ndjson` |
| 05-tool-calls — Tool calls (read + search) | codex | PASS | `35dbef5d719e` | `codex/05-tool-calls.ndjson` |
| 06-edits — Edits / diffs in auto-accept mode | codex | PASS | `-` | `codex/06-edits.ndjson` |
| 07-permissions — Permission requests: allow, reject, and cancel-while-pending | codex | PASS | `-` | `codex/07-permissions.ndjson` |
| 07n-sandbox-denied — Negative control: sandbox-denied command, no escalation (codex) | codex | observed | `35dbef5d719e` | `codex/07n-sandbox-denied.ndjson` |
| 08-plan-events — Plan / todo events | codex | PASS | `-` | `codex/08-plan-events.ndjson` |
| 09-thoughts — Thought chunks at high effort | codex | observed | `-` | `codex/09-thoughts.ndjson` |
| 10-commands-config — Slash commands + config option switch | codex | PASS | `-` | `codex/10-commands-config.ndjson` |
| 11-usage — Usage updates | codex | PASS | `-` | `codex/11-usage.ndjson` |
| 12-cancel — Cancel mid-turn + process hygiene | codex | PASS | `35dbef5d719e` | `codex/12-cancel.inherited.ndjson`, `codex/12-cancel.low.ndjson` |
| 13-queue-vs-steer — Second prompt during a turn; steering | codex | FAIL | `35dbef5d719e` | `codex/13-queue-vs-steer.ndjson` |
| 14-load-resume — session/load and session/resume after adapter restart | codex | PASS | `-` | `codex/14-load-resume.p1.ndjson`, `codex/14-load-resume.p2.ndjson`, `codex/14-load-resume.p3.ndjson` |
| 15-crash — kill -9 mid-turn | codex | PASS | `-` | `codex/15-crash.ndjson`, `codex/15-crash.early-exit.ndjson` |
| 16-embedded-context — Embedded resource block + standing-context cost | codex | PASS | `-` | `codex/16-embedded-context.ndjson` |
| 19-concurrency — Two adapters on the same worktree | codex | PASS | `-` | `codex/19-concurrency.planner.ndjson`, `codex/19-concurrency.implementer.ndjson` |
| 20-client-death — Client closes stdin mid-turn (dashboard/server dies) | codex | PASS | `-` | `codex/20-client-death.ndjson` |
<!-- capture:end -->

## Envelope

One JSON object per line:

```json
{"seq": 12, "ts": "2026-09-01T20:01:02.345Z", "t": 1834, "dir": "in", "msg": {"jsonrpc": "2.0", "method": "session/update", "params": {…}}}
```

- `seq` — monotonically increasing per file.
- `t` — milliseconds since the adapter process was spawned.
- `dir` — `out` = client → adapter (requests, responses to permission requests,
  notifications like `session/cancel`); `in` = adapter → client.
- `msg` — the raw JSON-RPC frame. The only edit is that e-mail addresses are
  replaced with `[redacted-email]` at capture time (the account e-mail shows up
  in codex-acp's `authentication/status` reply and in some agent text).

## Volatile values

Nothing else is scrubbed; the following differ between recordings and must not
be asserted on literally: absolute paths under
`scripts/spike/acp/out/target-<adapter>` (one disposable clone per adapter) and
`scripts/spike/acp/out/perm`, `sessionId`, tool-call ids (`toolu_…`, `call_…`,
`exec-…`), `usage_update` numbers, `_meta["_claude/rateLimit"]`, timestamps,
and model output text. Structure —
method names, `sessionUpdate` kinds, field presence, `ToolKind`/status values,
permission option kinds, stop reasons — is what the fixtures document.

No tests consume these yet; phase 2 (`assignment-chat-single-agent`) will replay
them against the real ACP client.
